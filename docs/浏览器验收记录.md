# 浏览器验收记录

验收日期：2026-09-07（北京时间）。执行工具：Playwright CLI 驱动 Microsoft Edge 无头浏览器。桌面视口 1440 × 900，手机视口 390 × 844。验收环境为 `http://127.0.0.1:18787`，数据目录为 `.runtime/ui-acceptance`，与日常资料隔离。本记录只描述实际执行的动作与观察，不以页面存在推定外部集成已完成。

## 样本与账号边界

- 首次启动通过界面初始化 `qa_admin`（验收管理员）；测试凭据仅保存在忽略目录下，不写入本报告。
- 使用默认“企业制度知识库”。唯一上传原件为项目自建的 `tests/fixtures/parser-samples/采购制度.docx`（约 1.7 KB），不存在企业真实资料。
- 该文档包含两逻辑页：第 1 页为“采购金额超过十万元应履行审批程序”及项目/金额表格，第 2 页为“归档保存五年”。
- 未读取或输出模型密钥。模型配置页只显示已配置凭据，密钥输入栏为空。

## 已完成的真实操作

| 环节 | 执行动作 | 实际观察 |
| --- | --- | --- |
| 初始化 | 在界面创建首个管理员并进入工作台 | 登录成功，初始文件与知识统计为空 |
| 文件接入 | 选择默认知识库，通过上传窗口上传采购制度 DOCX | 显示解析进度，随后转为待审核 |
| 内容校对 | 打开文档详情，对照两逻辑页与表格，编辑摘要和标签并保存 | 内容完整；明确提示 DOCX 逻辑页与 Word 排版物理页的差异 |
| 审核发布 | 填写审核说明后发布 | 文档状态为已发布，版本 V1 |
| 知识搜索 | 搜索“采购金额超过十万元” | 返回采购制度的两个段落，带文档版本、页/节与定位原文入口 |
| 真实问答 | 提问采购审批要求与归档保存期限 | DeepSeek 实际返回答案及两处引用，详见下一节 |
| 引用核验 | 点击第 2 处引用 | 进入相同文档并携带对应段落编号和 page=2 定位参数 |
| 原件核验 | 点击“查看原件”下载 DOCX | 下载文件与上传样本 SHA-256 一致 |
| 历史问答 | 离开问答页后重新打开历史会话 | 先前答案及两处引用仍可读取 |
| 反馈 | 对答案提交“其他”反馈 | 反馈中心产生待处理记录，保留原问题及说明 |
| 治理 | 打开审核与治理页面 | 显示缺少复审日期、解析待核验两项真实问题 |
| 组织与人员 | 打开组织人员页 | 当前验收管理员及启用状态可见 |
| 模型配置 | 打开平台设置 | DeepSeek 兼容服务与已配置凭据状态可见，未发起额外连接测试 |
| 运维备份 | 点击创建备份 | 成功生成 2026-09-07 21:47 的备份记录，文档数为 1 |
| 质量评测 | 创建一个采购问题，指定采购制度与关键词“履行审批程序”，运行检索评测 | 1/1 完成，规则通过率 100%；本次为单一样本回归，不代表整体质量指标 |
| 应用服务 | 访问应用访问授权、事件通知、统一登录三个页签 | 页面可访问；测试环境未配置外部应用、接收地址或企业身份提供方，未声称完成实际企业接入 |
| 手机入口 | 在 390 × 844 视口打开并使用导航进入文件列表 | 抽屉导航可打开且跳转后关闭；文件名、知识库和发布状态可见 |

## 实际模型结果

已执行 1 次真实模型问答。服务配置页显示模型 `deepseek-v4-flash`。提问为：

> 根据采购制度，采购金额超过十万元需要做什么？归档资料保存多久？

实际回答为：

> 根据采购制度，采购金额超过十万元应履行审批程序[1]。归档资料保存五年[2]。

页面标记为“基于原文生成”，引用 1 指向第 1 逻辑页采购审批条款，引用 2 指向第 2 逻辑页归档期限。此次只验证该合成样本的实际调用、答案与引用链路；复杂业务答案质量和大规模并发不在此次浏览器验收范围。

## 截图证据

下列截图均已实际打开查看。路径相对项目根目录。

| 截图 | 内容 | 视口 |
| --- | --- | --- |
| `output/playwright/acceptance/01-desktop-home.png` | 首次登录工作台 | 1440 × 900 |
| `output/playwright/acceptance/02-desktop-files-review.png` | 上传后的待审核文件列表 | 1440 × 900 |
| `output/playwright/acceptance/03-desktop-document-detail.png` | 文档内容与审核详情 | 1440 × 900 |
| `output/playwright/acceptance/04-desktop-search.png` | 采购条款搜索结果 | 1440 × 900 |
| `output/playwright/acceptance/05-desktop-answer.png` | 实际模型答案和两处引用 | 1440 × 900 |
| `output/playwright/acceptance/06-desktop-feedback.png` | 已提交的反馈记录（修复前） | 1440 × 900 |
| `output/playwright/acceptance/07-desktop-evaluation.png` | 单条检索评测完成 | 1440 × 900 |
| `output/playwright/acceptance/08-mobile-home.png` | 手机工作台 | 390 × 844 |
| `output/playwright/acceptance/09-mobile-navigation.png` | 手机导航抽屉 | 390 × 844 |
| `output/playwright/acceptance/10-mobile-files.png` | 手机文件列表 | 390 × 844 |
| `output/playwright/acceptance/11-mobile-document.png` | 最新手机文档详情与索引入口 | 390 × 844 |
| `output/playwright/acceptance/12-mobile-document-content.png` | 手机本地语义模型与索引时间 | 390 × 844 |
| `output/playwright/acceptance/13-mobile-document-text.png` | 手机完整两段解析正文 | 390 × 844 |
| `output/playwright/acceptance/14-mobile-answer.png` | 重启后手机历史问答及两处引用 | 390 × 844 |
| `output/playwright/acceptance/15-desktop-feedback-fixed.png` | 中文类型修复与已解决反馈 | 1440 × 900 |
| `output/playwright/acceptance/16-desktop-operations.png` | 平均接口处理耗时与已有备份 | 1440 × 900 |
| `output/playwright/acceptance/17-desktop-hybrid-search.png` | 改写业务问题的本地混合检索结果 | 1440 × 900 |

桌面主要内容未发现遮挡、重叠。手机文件表格使用内部横向滚动；首屏可以直接点击文档标题，后续列可滚动查看。文档表格以结构化文本展示，当前不等同于完整还原 Word 版式。

## 发现问题与后续复验

首轮发现治理筛选项、反馈类型和模型能力名称存在未映射的英文值，以及运行保障平均耗时显示不完整。对应实现负责人已修复。2026-09-07 21:53—21:58 在服务重新构建并重启后完成最终复验：

- 治理类型显示“缺少复审日期”“解析待核验”；反馈类型显示“其他问题”；模型能力显示“扫描件识别”“混合检索”“服务端权限校验”等中文名称。
- 反馈办理已填写核验结论并保存，状态由待处理变为已解决，处理结论在列表可见。
- 运行保障显示平均响应 `4 ms`，并注明这是本次运行已完成请求的平均处理耗时，不能解读为模型问答耗时。
- 已发布采购文档自动获得本地 BGE 中文语义索引。实际点击“重建语义索引”，先显示“正在构建”，随后自动恢复“已就绪”，更新时间更新至 21:53；V1、两段正文及人工修订均保留。
- 改写问题“购买软件花费超过十万，应该走什么手续？”实际检索返回第 1 页审批条款。浏览器已完成的搜索响应明确返回策略“中文 BM25 + 本地 BGE 中文语义检索，RRF 融合排序”，命中原因“中文 BM25 关键词匹配 + 语义相似”，不是仅以配置状态推断语义能力。
- 手机文档详情、索引信息、原件入口、两页正文、历史问答及两处引用均已截图并实际查看，没有遮挡或页面横向溢出。文档页与问答页测得页面宽度为 390，等于视口宽度 390。

本次实际浏览器验收共保存并查看 17 张截图。真实模型调用总数仍为 1 次，复验通过已保存会话读取答案，没有追加模型调用。未在浏览器验收中接入真实 SSO、Webhook、外部资料来源或业务应用；这些外部联调须由企业提供对应配置后单独验收。

最终控制台检查：错误 0，警告 0；记录见 `output/playwright/acceptance/console-errors.txt` 与 `console-warnings.txt`。平台设置页仅有浏览器 VERBOSE 级别的表单 autocomplete 建议，不属于应用运行错误。浏览器操作中曾遇到一次状态变化后的旧元素定位失效，以及一次已改名的 CLI 命令；均刷新定位或改用当前命令完成，不计为应用错误。

